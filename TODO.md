# TODO

+ Check when are settings synced, should be on each route change, song play or on some small interval

+ Fetch the current version from the github repo and if bigger than local one show indicator that it needs update

## Known issues
+ Fix mobile playback issues (low priority)
