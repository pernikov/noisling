{
  "name": "noisling",
  "version": "0.5.0",
  "private": true,
  "description": "Local music streaming app",
  "scripts": {
    "start": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "start:server": "pnpm --filter server dev",
    "start:client": "pnpm --filter client dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}